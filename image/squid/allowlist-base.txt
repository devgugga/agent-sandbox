# image/squid/allowlist-base.txt
# Um dominio por linha. Prefixo "." casa subdominios.
# APIs dos agentes
.anthropic.com
.openai.com
.googleapis.com
# O agy faz um eligibility check que busca a foto de perfil do usuario; sem
# este dominio ele falha no arranque com "Forbidden", apesar de autenticado.
.googleusercontent.com
.antigravity.google
# Servico de feature flags do Antigravity. Sem ele o agy aborta o arranque
# com "authentication required" — mesmo estando autenticado no keyring.
antigravity-unleash.goog
# codigo
.github.com
# pacotes
registry.npmjs.org
# O mise e o caminho declarado para ferramentas de projeto (spec §8); ele
# resolve versoes e baixa binarios daqui. Sem este dominio, um projeto com
# mise.toml falha no arranque do workspace.
mise.jdx.dev

# O Orca instala o relay dele DENTRO do container (~/.orca-remote) e o
# node-pty compila via node-gyp, que busca os headers do Node aqui. Sem este
# dominio o workspace da receita falha no arranque com "Request was cancelled".
nodejs.org
# Assets de release do GitHub (prebuilds nativos) resolvem para este host.
.githubusercontent.com
