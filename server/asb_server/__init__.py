# Placeholder package so `uv sync` can resolve the `server` workspace member.
# Task 3 replaces this with the real FastAPI daemon (`create_app`, routes,
# `asb_server.app`). Nothing here yet — see docs/superpowers/specs/
# 2026-09-22-asb-web-foundation-design.md section 7.
