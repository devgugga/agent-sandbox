"""server/asb_server/api — the daemon's route modules, one per resource.

`auth.py` (Task 4) is the only router wired in so far; `health.py` and
`tree.py` (Task 5) and `static.py` (Task 6) slot in the same way.
"""
